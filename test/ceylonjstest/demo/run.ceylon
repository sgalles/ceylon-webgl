import ceylonjs.demo {
    trompon,
    Image
}
"Run the module `ceylonjstest.demo`."
shared void run() {
//    value s = trompon.image;
    print(Image(trompon.raw));
    //value lines = trompon.raw.split('\n'.equals).filter((s) => !s.trimmed.empty).sequence();
    
    //print(lines);
      
}